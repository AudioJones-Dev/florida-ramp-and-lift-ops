# SOPs

This folder contains standard operating procedures.

The existing repo convention is `docs/sop/` singular, so new SOP placeholders are added here instead of creating a duplicate `docs/sops/` folder.

Existing SOPs in this folder remain preserved and authoritative until explicitly revised.
